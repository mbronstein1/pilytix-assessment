import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

/* Time invested
    Research/planning -> 2 hours
    Coding -> 6 hours
    Cleanup -> 1 hour
*/

/* 
Matthew Bronstein
mbronstein1@gmail.com
I testify that I completed this assessment without any help from
any outside sources other than resources I found via the web.
*/
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
